/*   if((!digitalRead(buttonPin)) && ((millis()-timerButton)>1e3)){
      buttonLEDstate=!buttonLEDstate;
      digitalWrite(buttonLED,buttonLEDstate);
      ++tankState;
      if(tankState>TANK_FULL) tankState=TANK_CRITICAL;  
       Serial.printf("%d",tankState);
      timerButton=millis();
    
  } */

/*   if ((millis() - timer) > PUBLISH_FREQUENCY) // triggers the routine every 5 seconds
  {
    
    ubidots.add(VARIABLE_LABEL, tankState); // Insert your variable Labels and the value to be sent
    ubidots.publish(DEVICE_LABEL);
    timer = millis();
  } */  